function fetchGradeData() {
  console.log("fetchGradeData called - will fetch data from PostgreSQL DB in future.");
}

function populateGradebook() {
  console.log("populateGradebook called - will populate the table with data.");
}

// Call the functions to test
fetchGradeData();
populateGradebook();
